<%= form_with(model: @tweet, id: dom_id(tweet)) do |form| %>
  <% if @tweet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
        <% @tweet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, class: "block mt-2" %>
    <%= form.textarea :body, class: "border w-full rounded-lg px-2 py-2" %>
  </div>

  <div>
    <%= form.submit class: "bg-blue-600 rounded-lg px-2 py-2" %>
  </div>
<% end %>
